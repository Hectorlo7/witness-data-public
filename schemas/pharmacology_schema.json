{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WITNESS DATA FACTORY — Pharmacology Record Schema",
  "type": "object",
  "required": ["id", "domain", "text", "labels", "eval"],
  "properties": {
    "id": { "type": "string", "pattern": "^PHAR-[0-9]{6}$" },
    "domain": { "type": "string", "const": "pharmacology" },
    "text": { "type": "string", "minLength": 50 },
    "labels": {
      "type": "object",
      "properties": {
        "event_type": { "type": "string", "enum": ["Adverse Drug Reaction", "Drug Interaction", "Drug Toxicity", "Drug Side Effect"] },
        "icd10": { "type": "string" },
        "severity": { "type": "string", "enum": ["Mild", "Moderate", "Severe", "Critical"] }
      }
    },
    "eval": {
      "type": "object",
      "required": ["confidence"],
      "properties": { "confidence": { "type": "number", "minimum": 0.97 } }
    }
  }
}
